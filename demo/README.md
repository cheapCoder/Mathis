# Demo

- `next-intl/`：嵌套 JSON + `useTranslations("ns")` 写法。单独打开：`code demo/next-intl`。在 Mathis 仓库窗口里也能用：仓库 `.vscode/settings.json` 已把 `mathis.define / apply / i18nLib` 指向它
- `react-intl/`：扁平 JS 定义 + `formatMessage({ id })` 写法。单独打开：`code demo/react-intl`

hover 前等右下角状态栏从 `Mathis 索引中` 变成 `Mathis`；显示 `未找到定义文件` 说明当前工作区根目录下没匹配到多语言文件。
